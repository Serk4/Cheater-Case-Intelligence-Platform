import { Typography } from '@mui/material';

export default function Dashboard() {
  return (
    <>
      <Typography variant="h5" gutterBottom>
        Dashboard
      </Typography>
      <Typography variant="body1">
        CCIP frontend shell is running.
      </Typography>
    </>
  );
}
